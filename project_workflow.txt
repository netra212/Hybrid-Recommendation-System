Goal: Build Hybrid Recommender System. 

Reason: To Increase UserEngagement, UserRetention.

Personalized, Diverse -> Hybrid Recommender System
    - Personalized Recommendation - Gives by Content Based. 
    - Diverse Recommendation - Gives by Collaborative Filtering.

Phase I -> Only Content Based. 
Phase II -> Only Collaborative
Phase III -> Combine above both by Weighted Approach 
    = Alpha1 * ContentBased + Alpha2 * CollaborativeBased 

Work:
By calculating similarity between the songs (metadata about the songs: tags, artist, attributes[temp, valence, dancebility, key]).

Song A:
    - 1st Vectorized. 
    - [0.9, 0.3] <- 2D Vectors
Similar with 
Song B:
Song C:
Song D:

Then caculates the similarity score, 
k(x, y) = x.y^T / ||x|| ||y||

Now, calcualtes the similarity of Song A with each other. 
        Song A   Song B   Song C    Song D
Song A 
Song B
Song C 
Song D 

similarity_vector [1, ., ., ., .,]
then sort the vector, and fetch the top_5 or top_10. 

In songs dataset -> 50K. 
56% of value of genre columns are missing. so drop this columns. 

steps. 
1. Data Cleaning: 
    - drop duplicates. 
    - missing values - genres column drops. 
    - tags - no tags. 
    - name, artist, tags - convert to lower case. 

# While building the ContentBased -->
    - Looks for similar attributes

so, transformations apply to dataset --> Vectorize --> Matrix. 
similarities -> cosine similarity 
then recommend top similarity scores. 